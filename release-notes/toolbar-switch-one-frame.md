- Switching to and from the Study screen no longer animates the top toolbar
  over a third of a second: the toolbar changes at once, in the same frame as
  the new screen. Going back to the deck list, the bottom bar changes height
  once, with the deck list, instead of twice before it.
