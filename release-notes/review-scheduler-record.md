Every review you answer now records which algorithm scheduled it: FSRS-7,
RWKV-Curve or RWKV-Instant. Nothing shows it yet. It is the data a later stat
needs, to compare how well an algorithm does on all reviews with how well it
does on the reviews it scheduled itself. Reviews answered before this version
have no record, because the review log never said which algorithm set their
intervals.
