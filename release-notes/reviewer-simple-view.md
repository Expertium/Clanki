- The reviewer's More menu follows the Simple | Advanced switch. Simple mode
  keeps Suspend Card, Options, Card Info, Tag Note, Delete Note, Replay Audio
  and Pause Audio; everything else is in Advanced mode. Every item keeps its
  keyboard shortcut.
- Flags are Advanced-only everywhere (the More menu, the Browser's Flag
  submenu and its Flags sidebar section); a card's existing flag is still
  shown. Tags do the same job and stay in Simple mode.
- "Mark Note" is now "Tag Note", and the Browser's "Toggle Mark" is "Toggle
  Tag": marking adds the tag "marked", which keeps its name.
