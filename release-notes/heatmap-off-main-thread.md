- The Review Heatmap no longer freezes the main window. The deck list and a
  deck's overview compute it in the background, together with their due
  counts, so the window stays responsive while it is counted; the first
  heatmap of a deck (and at start-up) is also about twice as fast, and a
  heatmap that has not changed is not checked against every card again.
