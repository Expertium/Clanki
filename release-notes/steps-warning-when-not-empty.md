- Deck options show a warning under the Learning steps and Relearning steps
  fields whenever the field is not empty: "It's recommended to keep the
  (re)learning steps field empty." Before, it showed only for steps of a day
  or more, so a relearning step of 1m got no warning. The text names no
  algorithm, because FSRS-7, RWKV-Curve and RWKV-Instant all handle same-day
  reviews.
