- The deck list keeps its scroll position when it refreshes. The 10-minute
  timer, a sync, a return of the focus to the window and a change of the
  decks or of a preset update the counts and the heatmap in place instead of
  jumping back to the top. Opening the deck list from another screen still
  starts at the top.
