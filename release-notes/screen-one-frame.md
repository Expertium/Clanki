- Opening a deck or going back to the deck list no longer shows half-built
  frames: the review heatmap appears complete (calendar, stats line and its
  buttons in one row) together with the bottom bar's new buttons, instead of
  building up over two or three frames.
