- On Windows, Clanki now uses a different build of its sound player, mpv. The
  old build sometimes hung when it started on a busy computer, and then cards
  had no sound until Clanki restarted. In a test with a busy computer, the old
  build hung in 31 of 40 starts and the new build in none. It is the same mpv
  version, so sounds play the same way.
