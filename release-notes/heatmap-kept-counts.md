- The review heatmap of the first deck you open now appears much sooner after
  Clanki starts. Clanki keeps the heatmap's counts of your older reviews in a
  file beside the collection (`collection.heatmap-cache.json`), so a new
  session no longer counts the whole review history again.
