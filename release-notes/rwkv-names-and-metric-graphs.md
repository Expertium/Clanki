- RWKV's values always name their algorithm: the Stats Retrievability graph,
  the deck menu, the deck-options box and settings, the new-card orders, the
  Browser's "calculating" title and the filtered-deck error say RWKV-Curve or
  RWKV-Instant, never just RWKV.
- Clicking a bar of the Retrievability graph opens the Browser on the
  collection's own algorithm's R (Shift+click no longer switches to FSRS-7's).
- The AUC-ROC, Calibration and Universal Metric+ graphs have a grid; the
  Calibration graph's bars no longer overlap; "No data" no longer shows over a
  graph that has data; the UM+ graph is titled "Universal Metric+
  cross-comparison", and its and the AUC texts are clearer.
