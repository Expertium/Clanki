- The deck options window opens about 2.5 times as fast (on a large
  collection, from about 690 ms to about 260 ms until the page is ready).
  Clanki keeps one deck options page loaded in the background, which costs
  about 50 MB of memory; the settings are still read fresh from the
  collection every time the window opens.
