- The Total Knowledge graph under RWKV opens much faster after its first run.
  It keeps the days it has already computed, also across restarts, and only
  computes the days since then. The numbers are exactly the same as before.
