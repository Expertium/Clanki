- Help > Check for Updates no longer errors out before Clanki has published
  its first release; it correctly reports "No updates available." instead.
- A build made from source is no longer always told an update is available
  regardless of its version; only a release that is actually newer than the
  installed build is offered.
