- Under RWKV-Instant, a review card that was never rated (for example after Set
  Due Date on a new card) shows "No prediction" in card info, and the Browser,
  Stats and `prop:rwkv:r` searches have no value for it. Before, it showed an R
  that never changed with time. The study queue still schedules such a card as
  before.
