- Enabling or installing the Review Heatmap add-on now shows that Clanki
  already has the heatmap built in (Preferences > Review Heatmap), and the
  add-on stays disabled. Before, it could be enabled again and was then
  disabled silently at the next start.
