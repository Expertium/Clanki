- Typing in the Browser sidebar's filter box is about twice as fast, and the
  sidebar redraws itself faster after a change (a sidebar of 1,390 items: one
  keystroke 19 to 10 ms, the redraw after a refresh 7.7 to 2.8 ms). The
  sidebar shows the same items, expanded the same way.
