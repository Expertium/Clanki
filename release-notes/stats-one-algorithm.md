- The Stats page shows only the active algorithm. Under RWKV, the
  Retrievability graph has no FSRS-7 line or FSRS-7 values; RWKV-Curve shows
  the Curve's R. While RWKV is still calculating, the graph says
  "Calculating…" and keeps waiting instead of falling back to FSRS-7. RWKV
  collections show no Difficulty graph, and RWKV-Instant no Stability graph.
