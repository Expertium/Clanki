- A collection that Anki schedules with SM-2 (FSRS off) switches to RWKV-Curve,
  with FSRS on, the first time Clanki opens it. Every card's memory state is
  computed from its review history; no due date changes. A collection that
  already uses FSRS keeps FSRS-7.
