- The Stats window loads faster. In Simple mode (the default) it now asks
  only for the graphs it shows, and no longer has RWKV score every card of
  the deck for the Retrievability graph it does not show: on a big
  RWKV-Curve collection that took minutes before anything was drawn. In
  both modes a big deck's review history is read faster, and under RWKV
  no FSRS-7 presets are prepared.
