- A deck's overview opens faster: its due counts and the check for buried
  cards (the Unbury button) now read only the cards they need, instead of
  every card in the collection (a 159k-card collection: 84 to 6 ms). The deck
  list's due counts are about 3 times faster too (44 to 13 ms).
