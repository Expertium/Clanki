- FSRS-7 now optimizes its parameters by itself. Each preset has "Optimize
  every N days" (30 by default, 0 = never) in the FSRS advanced section of
  deck options. Clanki runs the optimization in the background when you are
  not using it, and the "Time to optimize" reminder shows only for presets
  set to 0.
