- Clanki no longer has custom scheduling. Preferences no longer shows the
  "Custom scheduling" box, and a script stored by Anki no longer runs. The
  script stays in the collection, so Anki still gets it after a sync.
- Answering an RWKV-Curve card now keeps the card's custom data, and the log
  no longer gets a false "changed current state" warning for these cards.
