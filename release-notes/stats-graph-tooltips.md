- Stats: the UM+ and calibration graphs keep their details in the (?) tooltip
  next to the title, and the UM+ line under the graph now says that against the
  perfect oracle any imperfect algorithm has a slope of 1. The calibration
  graph says "Actual retention" instead of "Actual recall" and "Predicted
  retention" instead of "Predicted retrievability", and its bars are easier to
  see.
