- The FSRS Helper add-on stays disabled: it is not compatible with FSRS-7, the
  version of FSRS that Clanki uses. An enabled copy is disabled at start-up
  with a one-time notice, and enabling or installing it shows a message.
