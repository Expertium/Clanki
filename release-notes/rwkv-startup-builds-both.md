- When the local RWKV state is missing at profile open, Clanki now builds
  the state and the calibration data at once, without asking first and
  without a window; a short message says when it is done.
