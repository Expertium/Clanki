- Browser: Cards > Grade Now under RWKV-Curve gives each card the interval
  and stability RWKV-Curve gives it in the reviewer, with the same fuzz,
  instead of FSRS-7's. A card RWKV-Curve has no intervals for yet (for
  example while the RWKV state is still loading) is left ungraded, and the
  message after grading says how many. Under RWKV-Instant, Grade Now now
  writes the same review-log data as an answer in the reviewer.
