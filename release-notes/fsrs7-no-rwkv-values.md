- In an FSRS-7 collection, card info and the Stats page show no RWKV values,
  even with an RWKV model loaded (before, card info in the reviewer showed
  "RWKV computed R" and "FSRS (RWKV disabled)" rows).
