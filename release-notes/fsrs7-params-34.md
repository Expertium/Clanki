- FSRS-7 has 34 parameters. The backend call that builds the FSRS-7
  review-retrievability calibration data (no Clanki screen uses it; add-ons
  can) now runs FSRS-7 with the default parameters when it gets FSRS-6 or
  pre-release FSRS-7 parameters. Before, it ran FSRS-6 on them.
