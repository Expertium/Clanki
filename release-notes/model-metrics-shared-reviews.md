- Stats, model-quality graphs (AUC-ROC, Calibration, UM+): with two or more
  algorithms, every one is scored on the same reviews, the ones all of them
  predicted, instead of each on its own; the first review of each card is
  left out; and RWKV uses its newest stored prediction of each review.
