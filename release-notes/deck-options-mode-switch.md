- Deck options have the Simple | Advanced switch too, at the top right. It
  switches the page at once and is the same setting as the main window's
  switch, so there is no need to close and reopen deck options.
